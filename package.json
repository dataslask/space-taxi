{
  "name": "draw",
  "version": "1.0.0",
  "description": "Exploring canvas",
  "main": "index.js",
  "scripts": {
    "start": "beefy index.js --live",
    "build": "mkdir build -p && cp index.html build && browserify index.js -o build/index.js",
    "clean": "rm -r build"
  },
  "author": "Erlend Rotvik",
  "license": "ISC",
  "browserify": {
    "transform": [
      "browserify-css"
    ]
  },
  "devDependencies": {
    "beefy": "^2.1.6",
    "browserify": "^13.0.1",
    "browserify-css": "^0.9.1",
    "gulp": "^3.9.1",
    "gulp-sourcemaps": "^1.6.0",
    "gulp-uglify": "^1.5.3",
    "gulp-util": "^3.0.7",
    "lodash": "^4.13.1",
    "vinyl-buffer": "^1.0.0",
    "vinyl-source-stream": "^1.1.0"
  }
}
